{
  "name": "credit-analyzer-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:analyzer": "python3 credit-analyzer/browser/build.py --out dist/analyzer"
  },
  "devDependencies": {
    "pyodide": "^314.0.5"
  },
  "description": "Builds the credit analyzer's browser bundle for GitHub Pages."
}
